# [PRD] 모바일 우선 연복리 계산기 및 시각화 대시보드

## 1. 프로젝트 개요

- **프로젝트명**: 모바일 우선 인터랙티브 연복리 계산기 (Compound Interest Visualizer)
- **목적**: 초기 투자금과 정기 적립금, 복리 주기, 세금 옵션을 반영하여 장기 투자 자산의 성장 추이를 직관적으로 시뮬레이션하고, 두 가지 투자 시나리오(A/B)를 한 차트에서 비교 분석할 수 있는 웹 애플리케이션 구축
- **핵심 가치**:
  - 모바일 터치 환경에 최적화된 직관적이고 빠른 입력 UX
  - Recharts 기반의 실시간 반응형 자산 성장 차트 대시보드
  - 시나리오 A / B 비교를 통한 투자 전략 의사결정 지원
  - 일반과세, 비과세, 세금우대(ISA) 등 한국형 금융 과세 체계 반영

---

## 2. 사용자 경험 (UX) 및 모바일 우선 원칙

1. **모바일 퍼스트 레이아웃 (Mobile-First)**:
   - 360px ~ 430px 모바일 뷰포트를 기본 타깃으로 설계.
   - 단일 열(Single Column) 흐름: 빠른 입력 카드 → 핵심 요약 지표 카드 → 시각화 차트 대시보드 → 연도별 상세 내역.
   - 데스크톱 화면 확장 시 좌측 입력 컨트롤러, 우측 대시보드 및 비교 차트의 2열 레이아웃으로 점진적 향상(Progressive Enhancement).
2. **입력 편의성**:
   - 모바일 숫자 패드 최적화 (`inputmode="numeric"`).
   - 빠른 원화 증감 버튼 (`+10만`, `+100만`, `+1천만`, `정정/초기화`).
   - 대표 수익률 프리셋 칩 (예적금 `3.5%`, 배당/채권 `5%`, 지수 ETF `8%`, 공격투자 `12%`).
   - 슬라이더와 숫자 입력 필드 간의 양방향 실시간 동기화.
3. **가독성 및 피드백**:
   - 한글 금액 환산 표기 (예: `150,000,000원` 입력 시 `1억 5,000만 원` 텍스트 실시간 보조 표시).
   - 세전/세후 수익 및 세금 공제액 분리 명시.

---

## 3. 핵심 기능 요구사항

### 3.1 복리 계산 엔진
- **입력 파라미터**:
  - 초기 원금 (KRW, 0원 이상)
  - 정기 적립액 (KRW, 0원 이상)
  - 적립 주기 (매월 적립 / 매년 적립 / 적립 없음)
  - 적립 시점 (월초/연초 납입 vs 월말/연말 납입, 기본: 초 납입)
  - 목표 투자 기간 (1년 ~ 50년, 기본 10년)
  - 연 예상 수익률 (-30.0% ~ 50.0%, 기본 7.0%, 음수 수익률/원금 손실 시뮬레이션 지원)
  - 복리 주기 (연복리, 월복리, 일복리, 기본: 월복리)
  - 이자소득 과세 옵션:
    - 일반과세 (15.4%: 소득세 14% + 지방소득세 1.4%)
    - 비과세 (0%)
    - 세금우대 / ISA (9.9%: 소득세 9% + 지방소득세 0.9%)
    - 사용자 정의 세율 (%)
    - *수익률 음수/손실 발생 시 이자 소득세는 0원 처리*
- **산출 지표**:
  - 총 납입 원금 (초기 원금 + 정기 적립 누적액)
  - 세전 총 이자/손익 (이자 및 복리 수익 합계)
  - 이자 소득세 (세금 공제액)
  - 세후 최종 수령액
  - 원금 대비 순수익률 (세후, %)
  - 원금 배수 (예: 원금의 2.3배 또는 0.85배)

### 3.2 시나리오 A / B 듀얼 비교 모드
- **비교 기능**:
  - 기본 모드: 단일 시나리오(시나리오 A) 상세 분석.
  - 비교 모드 토글: '시나리오 B 추가 비교' 활성화 시 시나리오 A와 B를 동시 설정.
  - 복사 기능: '시나리오 A를 B로 복사' 버튼으로 손쉬운 변수 차이(예: 수익률만 +2% 변경 등) 시뮬레이션.
- **비교 차트 & 지표**:
  - 동일한 타임라인(1년~N년)에서 시나리오 A와 시나리오 B의 자산 성장 곡선을 하나의 차트에 오버레이.
  - 두 시나리오 간의 최종 자산 격차, 이자 격차, 수익률 격차 카드 제공.

### 3.3 시각화 차트 대시보드
- **차트 컴포넌트 (Recharts)**:
  - 영역형 누적 차트(Stacked Area Chart) 또는 선형 차트(Line Chart) 토글.
  - 단일 시나리오: [납입 원금 누적], [복리 이자 누적], [총 자산] 영역 구분.
  - 비교 모드: [시나리오 A 총자산], [시나리오 B 총자산] 비교 곡선 표시.
  - 반응형 컨테이너 및 모바일 터치 툴팁(Tooltips) 제공.

### 3.4 연도별 상세 데이터 테이블
- 연차별 [누적 납입 원금], [해당 연도 이자], [누적 이자], [기말 평가액], [수익률] 표 제공.
- 모바일을 위한 접이식 아코디언/스크롤 카드 UI 및 CSV 데이터 내보내기/복사 기능.

### 3.5 데이터 보관 및 관리
- 브라우저 LocalStorage를 통한 마지막 입력값 및 시나리오 A/B 자동 저장.
- '기본값으로 초기화' 기능 제공.

---

## 4. 데이터 모델 및 수학적 계산 공식

### 4.1 복리 계산 공식 (월 적립 + 월복리 기준)

1. **초기 원금의 미래가치 ($FV_{principal}$)**:
   $$FV_{principal} = P \times \left(1 + \frac{r}{n}\right)^{n \times t}$$
   - $P$: 초기 원금
   - $r$: 연이율 (소수점)
   - $n$: 연간 복리 횟수 (월복리 = 12, 연복리 = 1)
   - $t$: 투자 기간(년)

2. **정기 적립금의 미래가치 ($FV_{contributions}$)**:
   - 기수불(월초 납입 기준):
     $$FV_{contributions} = PMT \times \frac{\left(1 + \frac{r}{n}\right)^{n \times t} - 1}{\frac{r}{n}} \times \left(1 + \frac{r}{n}\right)$$
   - $PMT$: 정기 적립액

3. **총 세전 자산 및 세전 이자**:
   - $TotalAmount_{pre-tax} = FV_{principal} + FV_{contributions}$
   - $TotalPrincipal = P + (PMT \times 12 \times t)$
   - $GrossInterest = TotalAmount_{pre-tax} - TotalPrincipal$

4. **세금 및 세후 자산**:
   - $TaxAmount = \max(0, GrossInterest \times TaxRate)$
   - $NetInterest = GrossInterest - TaxAmount$
   - $TotalAmount_{post-tax} = TotalPrincipal + NetInterest$

---

## 5. 기술 스택 및 아키텍처

- **빌드 도구**: Vite (빠른 HMR, 가벼운 번들링)
- **프레임워크**: React 18 / 19 + TypeScript (엄격한 타입 시스템)
- **스타일링**: Tailwind CSS (모바일 우선 유틸리티 클래스)
- **아이콘**: Lucide React
- **차트**: Recharts (반응형 SVG 차트)
- **상태 관리**: React State + 커스텀 훅 (`useCompoundCalculator`, `useLocalStorage`)
- **디렉터리 구조**:
  ```text
  src/
  ├── components/
  │   ├── Header.tsx              // 앱 헤더 및 테마/설정
  │   ├── CalculatorForm.tsx       // 입력 폼 (슬라이더, 빠른버튼, 프리셋)
  │   ├── SummaryCards.tsx        // 핵심 지표 요약 카드
  │   ├── ChartDashboard.tsx      // Recharts 인터랙티브 차트
  │   ├── ComparisonView.tsx      // 시나리오 A vs B 비교 분석 카드
  │   ├── DataTable.tsx           // 연도별 상세 테이블
  │   └── QuickAmountButtons.tsx  // 모바일 편의 금액 증감 버튼
  ├── hooks/
  │   ├── useCalculator.ts        // 계산 로직 및 상태 제어
  │   └── useLocalStorage.ts      // 로컬스토리지 동기화 훅
  ├── utils/
  │   ├── calculator.ts           // 순수 복리 계산 수학 엔진 및 연도별 집계
  │   └── formatters.ts           // 한글 단위(억/만), 통화(₩), 백분율 포맷팅
  ├── types/
  │   └── calculator.ts           // 입력 파라미터, 계산 결과, 시나리오 타입 정의
  ├── App.tsx
  └── main.tsx
  ```

---

## 6. 비기능적 요구사항 및 검증 기준

1. **성능**: 슬라이더 조작 시 60fps에 준하는 즉각적인 실시간 차트 업데이트 (불필요한 리렌더링 방지).
2. **번들 최적화**: Recharts 및 주요 라이브러리의 코드 스플리팅(`manualChunks`)을 통해 초기 로딩 청크 크기를 500kB 이하로 경량화.
3. **모바일 접근성**: 터치 타깃 최소 44x44px 확보, 모바일 브라우저 오버스크롤 및 확대 왜곡 방지.
4. **정확성**: 부동소수점 오차 방지 처리 및 금융 표준 공식과의 오차 1원 이내 검증.
5. **품질 검증**: 단위 테스트를 통해 단리/복리/손실/세금 계산의 수학적 정합성 보증.
